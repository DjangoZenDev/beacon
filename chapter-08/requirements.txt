# Beacon v0.8 — Chapter 8: Async Work and the Message Bus
#
# Chapter 8 adds the outbox pattern for reliable message publication,
# idempotency keys for at-least-once processing, Kafka integration,
# and the search service consumer.
#
# Prerequisites:
#   - PostgreSQL 16+ shard-0 through shard-3
#   - etcd3 running on localhost:2379
#   - Redis 7+ running on localhost:6379
#   - Kafka running on localhost:9092
#   - Elasticsearch 8.x for search indexing

# ── requirements.txt ──────────────────────────────────────────────
Django==5.1.0
psycopg2-binary==2.9.10
gunicorn==22.0.0
locust==2.31.0
django-redis==5.4.0
redis==5.2.0
celery==5.4.0
django-celery-results==2.5.1
requests==2.32.0
etcd3==0.12.0
grpcio==1.68.0
grpcio-tools==1.68.0
protobuf==5.29.0
djangorestframework==3.15.0
channels==4.2.0
kafka-python==2.0.2
confluent-kafka==2.6.0
